// Exercise 02
// Create brand new ReactJS application with `create-react-app` command.
// Create ReactJS application. Initial page should be Cart Checkout form with fields:
// - First Name - text input
// - Last Name - text input
// - Delivery (yes or no) - radio
// If Delivery === yes, specify address:
// - Country (use `countries.json`) - dropdown
// - State (use `states.json`) - dropdown - should depend on country
// - Cities (use `cities.json`) - dropdown - should depend on state
// - Add button with fetching geolocation of the browser API

// - Payment type (cash or card)
// If Payment type === card, show:
// - Card Number - input
// - Exp Date - input
// - CVV - input

// After user submit the form, data should be saved to localStorage.
